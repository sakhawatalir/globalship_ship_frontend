'use client'

// Unified header entrypoint
// Re-export the single header implementation so all pages can import from
// `@/components/layout/header`.
import HeaderElectronics from './header-electronics'

export default HeaderElectronics


